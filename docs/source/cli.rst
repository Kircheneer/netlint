Using the CLI
==============

The default command for ``netlint`` is ``lint``, meaning
that running ``netlint`` without any command supplied
runs ``netlint lint``.

.. click:: netlint.main:cli
   :prog: netlint
   :nested: full